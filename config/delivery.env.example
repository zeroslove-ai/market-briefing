# Market Briefing R1 delivery secrets — copy to a protected server env file.
# Never commit real values.

TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=
# Optional Telegram forum topic/thread id.
TELEGRAM_MESSAGE_THREAD_ID=

# Gmail SMTP. Use a Google App Password, not the normal account password.
GMAIL_SMTP_USER=
GMAIL_APP_PASSWORD=
EMAIL_TO=
# Optional; defaults to GMAIL_SMTP_USER.
EMAIL_FROM=
