# Market Briefing R1 EC2 delivery secrets — Telegram only.
# Never commit real values.
#
# Gmail is deliberately excluded. Email delivery is performed through the
# connected Gmail MCP / ChatGPT Work account using the generated
# *-gmail-mcp.json handoff artifact.

TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=
# Optional Telegram forum topic/thread id.
TELEGRAM_MESSAGE_THREAD_ID=
